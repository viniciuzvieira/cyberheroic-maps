import type { TSESLint } from "@typescript-eslint/utils";
declare const rule: TSESLint.RuleModule<"noDuplicateProps" | "noDuplicateClass" | "noDuplicateChildren", [
    {
        ignoreCase?: boolean;
    }?
]>;
export default rule;
