import { TSESLint } from "@typescript-eslint/utils";
declare const rule: TSESLint.RuleModule<"naming" | "capitalization" | "make-handler" | "make-attr" | "detected-attr" | "spread-handler", [
    {
        ignoreCase?: boolean;
    }?
]>;
export default rule;
